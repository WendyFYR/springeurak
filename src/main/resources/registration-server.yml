# Configure this Discovery Server
eureka:
  instance:
    hostname: cloud-eureka-server  #localhost
  client:  # Not a client, don't register with yourself
    registerWithEureka: false
    fetchRegistry: false
server:
  port: 8080   # HTTP (Tomcat) port